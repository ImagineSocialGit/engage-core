<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Reply profile bootstrap definitions
    |--------------------------------------------------------------------------
    |
    | Profiles are synchronized into Inbound Messaging database authority.
    | Existing clients may temporarily keep definitions at
    | messaging.reply_profiles; the sync/runtime compatibility fallback remains
    | until the portability/bootstrap pass moves every client source.
    |
    */
    'reply_profiles' => [],
];