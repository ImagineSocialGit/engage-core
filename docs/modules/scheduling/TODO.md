# Scheduling TODO

- [x] Phase 4B.4 / 22E2A foundation: add a neutral optional destination-verification transport seam, Scheduling-owned ephemeral challenge/proof lifecycle, bounded resend/attempt/rate limits, hashed verification codes, and an optional Messaging bridge without adding Messaging as a Scheduling dependency or touching marketing consent.
- [x] Phase 4B.4 / 22E2B enforcement: add the public issue/verify/resend step, keep challenge/proof authority in server-owned session/cache state, require and consume a valid offer/session-bound proof before the public offer can become a capacity-consuming `BookingHold`, revalidate offer/service/location/host/resource/capacity/travel state at the hold boundary, reject browser-authored verification authority, and preserve the no-Messaging path.
- [x] Add first-class Scheduling Project State transfer support as an optional schema-activated section: transfer durable configuration, appointments, attendees, lifecycle history, and Appointment-owned resource occupancy; keep slot offers, booking holds, and destination-verification challenge/proof state transient and outside the transfer contract.
- [x] Close Scheduling public-origin readiness validation: keep omitted SCHEDULING_APP_URL valid because public booking is optional, but report any non-empty malformed value as a setup:validate error instead of silently treating the deployment as intentionally unconfigured.
- [x] UX Phase / 23A1 first-use architecture: replace the empty zero-state dashboard with guided setup/readiness, make service creation the primary setup path, generate technical service/host keys and sort defaults server-side, simplify first-use service/staff creation, demote Resources to Advanced, and keep staff/provider setup optional unless the business needs explicit assignees.
- [x] UX Phase / 23A2 availability authoring: replace the rule-engine-first availability workspace with service-first regular hours, multiple ranges per day, special-date replacement hours, whole/partial time off, one-off-change removal, and live bookable-time testing while preserving the generalized availability/runtime precedence engine underneath and retaining advanced raw rule controls behind progressive disclosure.
- [x] UX Phase / 23A2B booking clarity follow-up: summarize consecutive fixed-duration bookable starts into human-readable start-time ranges while preserving exact authoritative slot selection; make attendee identity explicit with existing Contact, new-person Contact resolution/creation, and snapshot-only booking modes; surface validation prominently; and harden snapshot-only appointment replay identity.
- [x] UX Phase / 23A3A public booking closeout: replace the duplicated step/cockpit presentation with a clean client-themeable flow; group fixed start times by day period; carry verified contact details forward; use first/last name and normalized phone capture; record the communications disclosure; show location/preparation guidance; make completion confirmation-only; add privacy-safe Scheduling funnel definitions plus durable Appointment facts; and return availability test submissions to their results card.
- [x] UX Phase / 23A3B Scheduling report surface: project retained Scheduling browser observations and `scheduling.public_booking` facts into Reporting-owned daily metrics and expose booking funnel, validation/drop-off, availability/verification, service, attribution, and exact stable-ID ad-spend views in CRM Reporting.
- [x] UX Phase / 23A3C appointment format: replace service-facing phone/virtual/fixed/customer-site location authoring with the first-class In person / Remote appointment-format hierarchy; retain location type only as the internal commitment/snapshot projection; require a complete format before public booking; and make successful destination verification proceed directly into the capacity hold and contact-details step.
- [ ] UX Phase / 23A3D appointment communications: add actual appointment confirmation, reminder, and follow-up planning/delivery through Messaging-owned seams, with consent/purpose policy, templates, recovery, and delivery reporting. Do not make the public surface promise a message type until its client configuration and runtime are enabled.
- [ ] UX Phase / 23A3 maintenance cleanup: finish the terminology/progressive-disclosure pass across existing service, staff/provider, assignment, resource, and appointment maintenance surfaces; explain any unavoidable specialized terms in place.